ORG 1000H

NUM1 DB ?
NUM2 DB ?

ORG 2000H
    MOV AL, 1
    MOV BX, OFFSET NUM1
    INT 6
    MOV BX, OFFSET NUM2
    INT 6

    SUB NUM1, 30H
    SUB NUM2, 30H; OBTENEMOS EL VALOR NUMERICO

    MOV CL, NUM1
    ADD CL, NUM2

    CMP CL, 10

    JS DIG ; SI TIENE UN SOLO DIGITO

    SUB CL, 10
    MOV BYTE PTR [BX], 31H ; IMPRIMIMOS EL 1
    INT 7

DIG:MOV BYTE PTR [BX], CL ; IMPRIMIMOS EL DIGITO
    ADD BYTE PTR [BX], 30H
     
    INT 7

    HLT

END