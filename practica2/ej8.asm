ORG 1000H

NUM1 DB ?
NUM2 DB ?

ORG 2000H
    MOV AL, 1
    MOV BX, OFFSET NUM1
    INT 6
    MOV BX, OFFSET NUM2
    INT 6

    SUB NUM1, 30H
    SUB NUM2, 30H; OBTENEMOS EL VALOR NUMERICO

    MOV CL, NUM1
    MOV CH, NUM2

    CMP CL, CH

    JNS POS      ; SI NO HAY SIGNO (NUM1 > NUM2)

    MOV CH, CL     ; SWAPEAMOS LOS NUMEROS PARA  OTBENER EL VALOR ABSOLUTO
    MOV CL, NUM2

    MOV BYTE PTR [BX], 45; SIGNO -

    INT 7

POS:SUB CL, CH 
    ADD CL, 30H

    MOV BYTE PTR [BX], CL

    INT 7

    INT 0
    HLT

END